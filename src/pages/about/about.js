function About() {
  return (
    <span
      style={{
        fontSize: "10vw",
        fontWeight: 600,
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        height: "100%",
      }}>
      work in progress.
    </span>
  );
}

export default About;
